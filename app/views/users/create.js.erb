<% if flash[:error] %>
	$("#sign-up-modal .flash-modal").html("<%= escape_javascript(flash_messages) %>");
<% else %>
	$('#sign-up-modal').modal('hide');
	$(".nav.navbar-nav").html("<%= escape_javascript(render partial: 'layouts/navigation')%>");
<% end %>